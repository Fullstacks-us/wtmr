# Example environment configuration for the backend
# Copy this file to `.env` before deployment and adjust values

APP_PORT=5000
IMAGE_UPLOAD_DIR=./images
DATABASE_URL=sqlite:///./db/logs.db
ENABLE_SYNC=false
DEEPSEEK_API=http://<your-private-vds-ip>:<port>/query
